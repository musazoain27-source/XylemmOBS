import { Tracker } from '@/components/tracker';
export const metadata = {
  title: 'Check your report — XylemmOBS',
  robots: { index: false, follow: false },
};
export default function Page() {
  return <Tracker />;
}
